{
  "name": "slide-and-reveal",
  "version": "0.5.13",
  "description": "Obsidian plugin: scroll-to-reveal flashcard study for labeled images.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs --prod",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "obsidian": "~1.7.0",
    "typescript": "^5.9.0"
  }
}
